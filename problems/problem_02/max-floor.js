const maxFloor = input => {
    console.log("Input : "+floor)
    var b =1/2*(Math.sqrt(8*(floor+1))-1)
    console.log(Math.floor(b));
};

const floor = 4000 ;
maxFloor(floor);

module.exports = { maxFloor };
